extends layout

block content
    .container
        .py-5.text-center
            img.d-block.mx-auto.mb-4(src='https://fixer.io/fixer_images/fixer_money.png', alt='', width='100', height='100')
            h5= 'From ' + from + ' to ' + to 
            body= 'Average is ' + average + ' between dates ' + start_date + ' and ' + end_date